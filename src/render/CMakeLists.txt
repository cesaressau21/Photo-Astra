add_library(photo_astra_render STATIC UnavailableRenderer.cpp)
add_library(PhotoAstra::Render ALIAS photo_astra_render)
target_include_directories(photo_astra_render PUBLIC include)
photo_astra_target_defaults(photo_astra_render)
target_link_libraries(photo_astra_render PUBLIC PhotoAstra::Core)
if(PHOTO_ASTRA_BUILD_UI)
    include(${PROJECT_SOURCE_DIR}/cmake/Skia.cmake)
    target_sources(photo_astra_render PRIVATE SkiaRenderer.cpp SkiaCompositor.cpp EffectPipeline.cpp)
    target_link_libraries(photo_astra_render PRIVATE Skia::Skia)
endif()
