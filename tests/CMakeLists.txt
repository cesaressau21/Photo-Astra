add_executable(core_tests DocumentTests.cpp)
target_link_libraries(core_tests PRIVATE PhotoAstra::Core PhotoAstra::Render)
photo_astra_target_defaults(core_tests)
add_test(NAME core.document COMMAND core_tests)

if(PHOTO_ASTRA_BUILD_UI)
    find_package(Qt6 6.5 REQUIRED COMPONENTS Test)
    qt_add_executable(ui_tests MainWindowTests.cpp)
    target_link_libraries(ui_tests PRIVATE PhotoAstra::Ui Qt6::Test Qt6::OpenGLWidgets)
    photo_astra_target_defaults(ui_tests)
    add_test(NAME ui.main_window COMMAND ui_tests -platform offscreen)
    set_tests_properties(ui.main_window PROPERTIES TIMEOUT 30)
    qt_add_executable(image_tests ImagePipelineTests.cpp)
    target_link_libraries(image_tests PRIVATE PhotoAstra::Application PhotoAstra::Render Qt6::Test)
    photo_astra_target_defaults(image_tests)
    add_test(NAME image.pipeline COMMAND image_tests)
    set_tests_properties(image.pipeline PROPERTIES TIMEOUT 30)
    qt_add_executable(project_tests ProjectFileTests.cpp)
    target_link_libraries(project_tests PRIVATE PhotoAstra::Application PhotoAstra::Render Qt6::Test)
    photo_astra_target_defaults(project_tests)
    add_test(NAME project.file COMMAND project_tests)
    set_tests_properties(project.file PROPERTIES TIMEOUT 30)
    if(WIN32)
        get_target_property(qt_qmake Qt6::qmake IMPORTED_LOCATION)
        get_filename_component(qt_bin "${qt_qmake}" DIRECTORY)
        set_tests_properties(ui.main_window image.pipeline project.file PROPERTIES
            ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${qt_bin}")
    endif()
endif()
